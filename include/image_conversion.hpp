#pragma once
#include <string>

// what... the computer is crazy
// but once again like the function isnt recognized in main for some reason?

// yeah i guess so

// oh it works now

namespace ImageConversion
{
	//we could always just char*
	std::string GenerateExpression(unsigned char* data, int target)
	{
		return "1+1=3";
	}
}
